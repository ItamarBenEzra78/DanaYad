import numpy as np
from scipy import stats
import sympy as sp

print("\n" + "="*60)
print("תרגיל 5 - השוואה בין שלושה אומדים")
print("="*60)

print("\nמודל: y_i = β*x_i + ε_i (ללא חותך)")
print("ε_i ~ N(0, σ²) בלתי מתואמים")
print("\nשלושה אומדים:")
print("a) β̂ = (∑x_i*y_i - n*x̄*ȳ) / (∑x_i² - n*x̄²)")
print("b) β̂ = (∑x_i*y_i) / (∑x_i²)")
print("c) β̂ = (∑y_i) / (∑x_i)")

print("\n" + "-"*60)
print("בדיקה לכל אומד:")
print("-"*60)

print("\n### אומד (a) ###")
print("זהו אומד OLS עם הנחה שצריך חותך!")
print("א. חסר הטיה? כן (זהו OLS)")
print("ב. אומד ריבועים פחותים? כן (זהו OLS)")
print("ג. שונות: Var(β̂) = σ² / (∑(x_i - x̄)²)")
print("   זהו האומד עם שונות קטנה ביותר מבין אומדים חסרי הטיה")

print("\n### אומד (b) ###")
print("זהו אומד ריבועים פחותים רגרסיה ללא חותך")
print("א. חסר הטיה?")
print("   E[β̂] = E[∑x_i*y_i / ∑x_i²]")
print("        = E[∑x_i*(β*x_i + ε_i) / ∑x_i²]")
print("        = E[(β*∑x_i² + ∑x_i*ε_i) / ∑x_i²]")
print("        = β + E[∑x_i*ε_i / ∑x_i²]")
print("        = β + 0 = β")
print("   כן, חסר הטיה ✓")

print("\nב. אומד ריבועים פחותים?")
print("   כן, זהו אומד OLS כשדוחים את ההנחה על חותך ✓")

print("\nג. שונות:")
print("   Var(β̂) = Var(∑x_i*y_i / ∑x_i²)")
print("          = ∑x_i²*Var(ε_i) / (∑x_i²)²")
print("          = σ² / ∑x_i²")

print("\n### אומד (c) ###")
print("זהו ממוצע פשוט של y חלקי ממוצע פשוט של x")
print("א. חסר הטיה?")
print("   E[β̂] = E[(∑y_i / n) / (∑x_i / n)]")
print("        = E[(∑(β*x_i + ε_i) / n) / (∑x_i / n)]")
print("        = E[(β*∑x_i / n + ∑ε_i / n) / (∑x_i / n)]")
print("        = E[β + (∑ε_i / n) / (∑x_i / n)]")
print("   זה לא בהכרח שווה ל-β (תלוי בנתונים)")
print("   לא, בדרך כלל יש הטיה ✗")

print("\nב. אומד ריבועים פחותים?")
print("   לא, זה לא ממזער את ∑(y_i - β*x_i)² ✗")

print("\nג. שונות:")
print("   הרבה יותר גדולה מאשר (a) או (b)")

print("\n" + "="*60)
print("סיכום השוואה:")
print("="*60)

# יצירת טבלה
comparison = {
    "אומד": ["(a) OLS עם חותך", "(b) OLS ללא חותך", "(c) ממוצעים פשוטים"],
    "חסר הטיה": ["✓ כן", "✓ כן", "✗ לא"],
    "OLS": ["✓ כן", "✓ כן", "✗ לא"],
    "שונות": ["σ²/Sxx", "σ²/∑x_i²", "גבוהה מאוד"],
}

print("\n" + "="*60)
print("אומד (a) הוא הטוב ביותר!")
print("="*60)
print("סיבות:")
print("1. חסר הטיה ✓")
print("2. אומד OLS ✓")
print("3. שונות מינימאלית (BLUE) ✓")
print("4. לא דוחה את הרגרסורים (כאשר צריך חותך)")

print("\nאומד (b) זה טוב כשנודעים בודאות שאין חותך")
print("אומד (c) לא טוב - בדרך כלל יש הטיה וגם שונות גדולה")

